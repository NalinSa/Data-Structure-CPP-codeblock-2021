#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int main()
{
    std::ios_base::sync_with_stdio(false); std::cin.tie(0);
    int n,w,num;
    cin>>n>>w;
    int index = 0;
    vector<priority_queue<int>> vp(n-w);
    for(int i = 0;i<n;i++){
        cin>>num;
        while(i)
    }

}
